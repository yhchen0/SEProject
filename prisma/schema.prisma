generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://yanhau:h97881992@localhost:5432/seproject?schema=public"
}

// model meetingtousers {
//   meetingId  Int      @map("meeting_id")
//   userId     String   @map("user_id")
//   created_at DateTime @default(now())
//   meetings   meetings @relation("meetingtousersTomeetings", fields: [meetingId], references: [id])
//   users      users    @relation(fields: [userId], references: [userId])

//   @@unique([meetingId, userId], name: "_meetingtousers_pair")
//   @@index([meetingId], name: "_meetingtousers_meeting")
//   @@map("_meetingtousers")
// }

model Users {
  userId         String           @id @map("user_id")
  userName       String           @map("user_name")
  createdAt      DateTime         @default(now()) @map("created_at")
  updatedAt      DateTime         @default(now()) @map("updated_at")
  meetings       Meetings[]

  @@map("Users")
}

model Meetings {
  id            Int         @id @default(autoincrement())
  roomId        room        @map("room_id")
  beginAt       DateTime    @map("begin_at")
  finishAt      DateTime    @map("finish_at")
  information   String?
  users         Users[]

  @@map("Meetings")
}

enum room {
  ROOM1
  ROOM2
  ROOM3
  ROOM4
  ROOM5
  ROOM6
}
